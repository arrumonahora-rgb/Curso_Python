# Curso_Python
Python

## GUI leve com tkinter

Adicionei um exemplo simples de interface gráfica usando o `tkinter` (já incluído no Python).

- Executar a GUI (PowerShell):

```powershell
python .\gui_tk.py
```

O arquivo `gui_tk.py` mostra uma mensagem e inclui uma calculadora de IMC simples.
